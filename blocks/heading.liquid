{% doc %}
  @param {boolean} [margin]
{% enddoc %}

{%- liquid
  if block.settings.text == empty
    continue
  endif
-%}

<{{ block.settings.heading_tag }}
  id="shopify-block-{{ block.id }}"
  class="text-{{ block.settings.heading_style -}} {% if margin %} heading-margin{% endif %}"
  {{ block.shopify_attributes }}
>
  {{ block.settings.text }}
</{{ block.settings.heading_tag }}>

{% schema %}
{
  "name": "t:theme_blocks.heading.name",
  "tag": null,
  "settings": [
    {
      "type": "text",
      "id": "text",
      "label": "t:presets.settings.text.label",
      "default": "Heading text"
    },
    {
      "type": "select",
      "id": "heading_tag",
      "label": "t:presets.settings.heading_tag.label",
      "options": [
        { "value": "h1", "label": "t:presets.settings.heading_tag.options.h1" },
        { "value": "h2", "label": "t:presets.settings.heading_tag.options.h2" },
        { "value": "h3", "label": "t:presets.settings.heading_tag.options.h3" },
        { "value": "h4", "label": "t:presets.settings.heading_tag.options.h4" },
        { "value": "h5", "label": "t:presets.settings.heading_tag.options.h5" },
        { "value": "h6", "label": "t:presets.settings.heading_tag.options.h6" },
        { "value": "p", "label": "t:presets.settings.heading_tag.options.p" }
      ],
      "default": "h1"
    },
    {
      "type": "select",
      "id": "heading_style",
      "label": "t:presets.settings.heading_style.label",
      "options": [
        { "value": "h1", "label": "t:presets.settings.heading_tag.options.h1" },
        { "value": "h2", "label": "t:presets.settings.heading_tag.options.h2" },
        { "value": "h3", "label": "t:presets.settings.heading_tag.options.h3" },
        { "value": "h4", "label": "t:presets.settings.heading_tag.options.h4" },
        { "value": "h5", "label": "t:presets.settings.heading_tag.options.h5" },
        { "value": "h6", "label": "t:presets.settings.heading_tag.options.h6" }
      ],
      "default": "h3"
    }
  ],
  "presets": [
    {
      "name": "t:theme_blocks.heading.name"
    }
  ]
}
{% endschema %}
